NOTE THIS ::
Always add .env variables from you cloud mysql service 
to you active Api service . Because some some cloud hosting dont share variables 
so variables are personal always add mysql env copy it and add to api service so php can read it 
thanks ..

Or better still make mysql env variables a shared variable so that it can be used across all services generally 
